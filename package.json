{
  "name": "7tube",
  "private": true,
  "version": "0.2.0",
  "description": "Repository foundation for the 7tube short-video platform.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "dev:api": "node services/api/src/server.js",
    "dev:web": "node -e \"console.log('Open apps/web/src/index.html in a browser, or run the API for live data.')\"",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=22"
  }
}
